# ─────────────────────────────────────────────────────────
# Z3 Real Estate — Environment Variables
# Copy this file to .env.local and fill in your values.
# Never commit .env.local to version control.
# ─────────────────────────────────────────────────────────

# Mapbox GL token (required for live interactive map)
# Get yours at: https://account.mapbox.com/access-tokens/
# Without this, the app uses the built-in SVG fallback map.
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ1IjoiWU9VUl9VU0VSTkFNRSIsImEiOiJZT1VSX1RPS0VOfQ.EXAMPLE
